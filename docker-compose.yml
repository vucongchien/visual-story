version: '3.8'

services:
  # Production service
  visual-story:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: visual-story-app
    ports:
      - "80:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production

